一、尚未初始化变量
代码如下：

 错误提示如下：

 错误分析及解决：
错误提示第4行有错误，并且小箭头指向的是a，说可能尚未初始化变量a。这个错误提示很明显就是a没有初始化的问题，我们的代码的第三行int a;该代码只是定义了变量a，但是a并没有被赋值，所以下面我们马上输出a的时候会报错，因为变量在使用前必须先赋值。所以解决方法如下：在使用a前，给变量a赋值，具体赋什么值看具体的需求，因为测试所以直接赋值0了。



总结：提示可能尚未初始化变量的话只要给改变量在使用之前赋值就可以了。具体赋什么值看具体需求。


 二、找不到符号
代码如下：

错误提示如下：

 错误分析及解决：
提示第4行有错误，说是找不到符号c（因为小箭头指向了c），因为变量使用之前必须定义并且赋值，所以我们在第4行之前去找c这个变量有没有定义赋值，最后发现我们只定义了a并没有定义c。这里可能是写错了，原本是想输出a的最后写错写成了c，只要把c改成a就可以了。还有可能是我还需要一个变量c的话，那么只要在第4行之前定义一个变量c并且在打印前赋值就可以了。
 

