成员{


* 静态内部类能不能有非静态成员？为什么？可以 静态内部类可以使用类调用 可以静态内部类只需要在外部类之前被加载就可以
 * 静态内部类能不能有静态成员？为什么？可以 没理由不可以
 * 静态内部类能不能访问外部类的非静态成员？不可以 静态内部类加载时外部类尚未加载
 * 静态内部类能不能访问外部类的静态成员？ 可以静态成员属于类,他们同时加载       * 非静态内部类能不能有静态成员 不可以 内部类静态成员应可以使用类名调用 (属于类),他不可附属于外部类对象    



} 
                                              public     大于                    protect             大于                  default              大于        private
同一类内                               可以                                 可以                                         可以                               可以
同一包内                                可以                               可以                                         可以
不同包内的子类中                可以                                   可以            
不同包中的非子父类中            可以        
想要调用不同包中的类需要先导包(除java.lang)  (导包即导类,以类名结尾,以"."作为分隔符)        *代表通配符将导入包中所有的类     
private 修饰的静态类不可以被类名调用 .请他权限修饰符号同理.    
局部内部类仅能调用局部的常量和成员的变量