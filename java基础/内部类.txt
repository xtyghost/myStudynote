定义在一个类中的类:3种
成员内部类（是一个类）:new outer().new Inner()
静态内部类 new outer.Inner()   
静态静态类内部类可以直接调用方法
静态内部类的加载与外部类同时
当成员内部类被private修饰时无法被外部调用
        局部内部类（是一个类）只能调用内部类所在方法的常量和成员变量         匿名内部类  (是对象):书写简单,可以作为变量传入,节省空间!(局部变量随方法的出栈而消失)当对象不被引用指向时会被当做垃圾




